apiVersion: v1
kind: Secret
metadata:
  name: file-config-{{APP}}-{{ENV}}
  namespace: {{NAMESPACE}}
  labels:
    app: {{APP}}
    env: {{ENV}}
type: Opaque
data:
  appsettings.{{DOTNET_ENV}}.json: {{CONFIG_B64}}
